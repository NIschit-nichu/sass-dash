{
  "name": "saas-dashboard-client",
  "version": "1.0.0",
  "description": "SaaS Dashboard Client",
  "scripts": {
    "start": "serve .",
    "build": "mkdir -p dist && cp -r *.html *.css assets dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "serve": "^14.2.1"
  }
} 